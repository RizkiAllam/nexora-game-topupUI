export const APP_CONFIG = {
  NAME: 'Nexora',
  VERSION: '1.0.0',
  DEFAULT_THEME: 'dark',
} as const;

export const ROUTES = {
  HOME: '/',
  GAMES: '/games',
  CHECKOUT: '/checkout',
  HISTORY: '/history',
  PROFILE: '/profile',
} as const;